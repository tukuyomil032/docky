pre-commit:
  parallel: true
  commands:
    swiftformat-lint:
      glob: "**/*.swift"
      run: swiftformat --lint --config .swiftformat {staged_files}
    swiftlint:
      glob: "**/*.swift"
      run: swiftlint lint --config .swiftlint.yml --strict {staged_files}
